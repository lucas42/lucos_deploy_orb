# This doesn't call out to another box because circleci runs on amd64
environment:
  ARCH: amd64
docker:
  - image: cimg/node:current
steps:
  - checkout
  - fetch-publish-creds
  - calc-version
  - publish-docker:
      port: "1234"
