parameters:
  host-domain:
    type: string
  architecture:
    type: string
docker:
  - image: circleci/buildpack-deps:buster
steps:
  - deploy:
      host-domain: << parameters.host-domain >>
      architecture: << parameters.architecture >>
