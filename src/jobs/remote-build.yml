parameters:
  build-domain:
    type: string
  ssh-port:
    type: integer
  architecture:
    type: string
docker:
  - image: circleci/buildpack-deps:buster
steps:
  - remote-build:
      build-domain: << parameters.build-domain >>
      ssh-port: << parameters.ssh-port >>
      architecture: << parameters.architecture >>
